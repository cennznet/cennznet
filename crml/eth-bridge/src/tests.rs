#[test]
fn prunes_expired_events() {}

#[test]
fn double_claim_fails() {}

#[test]
fn invalid_notarization_fails() {
	// receive invalid notarization should fail
}

#[test]
fn threshold_activation() {
	// validators submit session keys
	// offchain worker starts when threshold are registered
}
